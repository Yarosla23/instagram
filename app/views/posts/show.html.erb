<p style="color: green"><%= notice %></p>

<%= link_to "Back to posts", posts_path %>


  <p>
<div  class ="col-xl-5 col-md-6 col-sm-8 col-20  "style ="margin :auto">

          <div class="p-3 text-dark-emphasis bg-dark-subtle border border-dark-subtle rounded-3">
          <p class="card-text"><%=@post.title %>  </p>
            <p class="card-text"> <%= image_tag @post.picture.url,  style: "width: 300px; height: 400px;" if @post.picture? %> </p>
            <div class="card-body">

            <small class="text-body-secondary"> <%= @post.tags   %>  </p></small>
              <p class="card-text"><%= @post.description %>  </p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                    <div>
    <% if user_signed_in? %>
    <%like = current_user.likes.find_by(post: @post)%>
    <% if like.nil?%>
<%= button_to "like", likes_path,  params: {like: {post_id:
@post.id }}, method: :post, class: 'heart-button'%>
<%else%>
 <%= button_to "Unlike", like_path(like),  method: :delete%>
<%end%>
<%end%>
<p> <%= @post.likes.count%> </p>
</div>
                  <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
                  <button type="button" class="btn btn-sm btn-outline-secondary">  <%= link_to "Редактировать", edit_post_path(@post), class:"navbar-brand" %>  </button>
                </div>
                <small class="text-body-secondary"><%= @post.user.email %>  </p></small>
              </div>
            </div>
          </div>
        </div>


<% if user_signed_in? && current_user.id  == @post.user.id %>
    
<%end%>


<div>
<%= render "comments/form", comments: @comment %>
<% @post.comments.each do |comment|%>
<%=  comment.user_id%>
<%= comment.text%>
<% if !comment.text.blank?%>
 <%= button_to "Destroy this comment",  comment, method: :delete %>
 
<br>  
<%end%>
<%end%>
